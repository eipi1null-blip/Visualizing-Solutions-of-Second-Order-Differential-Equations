# General Coupled-System Phase Portrait

Upload `index.html`, `.nojekyll`, and this README to the root of the GitHub repository.

In **Settings > Pages**, select **Deploy from a branch**, the default branch, and `/ (root)`.

The applet is self-contained and has no external dependencies.
